// Módulo de Tarjeta de Contenido Personalizada
// Funcionalidad adicional si es necesaria

(function() {
  'use strict';
  
  // Inicialización del módulo
  function init() {
    // Aquí puedes agregar funcionalidad JavaScript adicional si es necesaria
    // Por ejemplo, animaciones, interacciones, etc.
  }
  
  // Ejecutar cuando el DOM esté listo
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();

